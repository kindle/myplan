import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reddah.today',
  appName: 'today',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
